---
name: "Commit"

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: Ubuntu-20.04
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
      - name: Run the goat
        uses: seisollc/goat@main
